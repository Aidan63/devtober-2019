/**
 * Order is important, should be multiplyable by 90 to get the correct direction.
 * 0 degrees should be left, then going around clockwise.
 */
enum Direction
{
    Up;
    Left;
    Down;
    Right;
}